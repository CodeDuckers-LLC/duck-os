This directory is part of the TinyFS rootfs source tree.

Files added here are copied into `build/rootfs/bin` before `tinyfs.img` is packed.
The build also stages these generated binaries here:
- hello.bin
- hello.elf
